/**
 * This file exists to maintain backward compatibility with
 * imports that use '../../components/ui/Button' instead of '../../ui/Button'
 */

import { Button } from '../../ui/Button';

export { Button };
export default Button;
